server:
  port: 8083
  servlet:
    context-path: /api/v1

spring:
  application:
    name: order-service
  data:
    mongodb:
      uri: ${MONGODB_URI}
      database: orders_db
  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false

jwt:
  secret: ${JWT_SECRET}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.edublitz: INFO

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:*}

services:
  product-service:
    base-url: ${PRODUCT_SERVICE_URL:http://localhost:8082/api/v1}
  user-service:
    base-url: ${USER_SERVICE_URL:http://localhost:8081/api/v1}
